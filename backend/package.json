{
  "name": "personal-finance-backend",
  "version": "1.0.0",
  "description": "Backend for Personal Finance Management App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step for backend'",
    "heroku-postbuild": "cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
